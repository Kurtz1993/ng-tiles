import { NgxCardBackDirective } from './ngx-card-back.directive';

describe('NgxCardBackDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxCardBackDirective();
    expect(directive).toBeTruthy();
  });
});
