import { NgxCardTitleDirective } from './ngx-card-title.directive';

describe('NgxCardTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxCardTitleDirective();
    expect(directive).toBeTruthy();
  });
});
