import { NgxCardBodyDirective } from './ngx-card-body.directive';

describe('NgxCardBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxCardBodyDirective();
    expect(directive).toBeTruthy();
  });
});
